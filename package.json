{
  "name": "torsade-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Torsade Technologies — landing page (static, deployed via GitHub Pages).",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
